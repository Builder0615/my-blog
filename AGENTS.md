# 项目工作规范

这份文件是本博客仓库的写作与协作规则。后续新增、修改或校验博客时，先执行下面的摘要，再按后面的详细条款核对。

## 写作要求摘要

1. **像作者在记录学习过程一样写。** 使用“我”“你”和自然的判断、取舍、踩坑，不写成冷冰冰的课程讲义、产品说明书或营销文案。
2. **篇幅和章节按项目复杂度决定。** 不为了模仿 Pi 或凑成 10 章而压缩内容；大仓库要拆成足够多的知识单元，并维护覆盖矩阵，明确哪些目录已经展开、哪些仍是边界。
3. **面向小白，但不牺牲技术准确性。** 第一次出现的术语先用白话解释，再给出正式名称；说明前置知识、真实入口、文件路径、函数名和运行链。
4. **每个功能都要回答完整的问题。** 说明它解决的约束、入口在哪里、实现方式和数据流、为什么这样设计、带来的好处、成本与替代方案、失败/边界路径，以及怎样在本地验证。
5. **以证据为中心。** 源码、用户指南和测试各自承担不同职责；源码链接固定到具体快照，代码示例必须真实可查。无法由源码证明的内容要标成推断，不能把猜测写成事实。
6. **代码和图必须说明证据类型。** 真实源码片段标出文件和函数；改写的流程必须明确标成“伪代码”；Mermaid 只表达已经核对过的关系，图下说明依据、能证明什么和不能证明什么，并在本地渲染验证。
7. **Q&A 只记录真实提问。** 只收录作者/读者在学习过程中实际提出的问题和回答，不预设、虚构或批量生成“可能会问的问题”；没有问题时保持空白记录区。
8. **外部资料只做对照和补充。** 可以引用官方文档、协议规范、作者文章和社区实践，但要标明官方/第三方/社区角色、链接和访问日期；外部资料不能替代当前源码快照，出现差异时记录版本和原因。
9. **跨项目比较要先分开求证。** 先分别说明 Pi、Grok Build 或其他项目的真实实现，再比较抽象概念；不用“更先进”“更简单”等空泛判断代替具体差异。
10. **安全边界必须写清楚。** 涉及权限、沙箱、认证、凭据和网络时，说明保护范围、平台/feature 条件、失败后的行为；不写“绝对安全”“完全隔离”等过度承诺。
11. **保持自然表达。** 不使用模板化标题和机械排比，不堆砌空话、重复概念或大段括号注释；每篇可以采用适合自身内容的结构。
12. **上游变化要同步修订。** 发现源码、用户指南或链接变化时，同时检查正文、索引、覆盖矩阵和 Q&A，记录修订原因，不能只改目录名。

## 图片输入兜底

如果当前模型不能接收图片输入，不得假装看到了图片。使用 /Users/xxx/.codex/skills/image-ocr-fallback/scripts/image_ocr <image-path> 做本地 OCR，再根据识别文本继续处理。

## 详细写作规则（所有博客通用）

下面的详细条款与前面的执行摘要共同生效，适用于目录、正文、索引和 Q&A 记录。

## 必须做到

1. 以作者本人的身份写作，像一个人在分享自己的学习过程、判断和踩坑记录，而不是课程讲义或说明书。
2. 使用自然的个人语气，可以写“我”“你”，可以有疑问、取舍和主观评价。
3. 保留技术事实的准确性，源码路径、链接、代码示例必须真实可查。
4. 图表用 Mermaid 时，必须先在本地渲染验证，确保浏览器里能正常显示。
5. 每篇博客都可以有自己的结构，不要套用同一套模板。

## 禁止出现

- 不要使用“本章学习目标”“内容点”“结论”“讲解”“学习价值”“小结”这类模板化标题。
- 不要用“首先/其次/最后”“综上所述”“总的来说”这类机械排比。
- 不要出现“作为一个AI”“由AI生成”“AI助手”等表述，也不要用营销腔、客服腔。
- 不要把每节都写成“结论 + 讲解 + 练习”的固定格式。
- 不要为了显得专业而堆砌空话、重复概念或大段括号注释。

所有博客，包括目录、正文和 Q&A 记录，都遵守这份要求。

## Grok Build 学习文档的补充约定

1. 每篇涉及源码的文章都标出仓库快照，源码链接尽量固定到具体 commit；不要把会被重新生成的根 `Cargo.toml` 当成主要修改入口。
2. 先把真实调用链对上，再写抽象概念。入口、crate、模块和函数名必须能相互对应；源码没有直接证明的内容，要明确写成我的推断或阅读方式。
3. 介绍 Rust workspace 时区分 crate 边界、运行时依赖、构建依赖和 feature gate，不把目录数量直接当成架构结论。
4. 涉及权限、沙箱、认证、网络和凭据时，同时写清保护范围、平台或 feature 条件，以及失败后的路径；不使用“绝对安全”“完全隔离”一类表述。
5. 用户指南、源码和测试承担不同职责：用户指南用于找入口，源码用于确认实现，测试用于确认行为。三者不一致时，记录差异和快照，而不是替其中一方补写不存在的行为。
6. Mermaid 只画已经从源码核对过的关系；图下面要附上对应的阅读路径或验证问题，避免把推测画成事实。
7. Rust 代码片段保留真实类型和函数名；为了说明流程而改写的内容必须标记为“伪代码”，不把伪代码伪装成可编译源码。

## 详细教程的补充要求

1. 章节数量由项目的实际复杂度决定，不为了和另一套文档对齐而强行凑成 10 章。一个独立的运行时边界、用户功能或前置概念都可以成为单独章节。
2. 面向小白写作时，第一次出现的术语要用白话解释，再给出 Rust、TypeScript 或协议里的正式名称。不能假定读者已经知道 async、actor、trait、token、turn、session、workspace 和 ACP。
3. 每个知识单元至少要回答“它解决什么问题、入口在哪里、数据怎么流动、失败会怎样、怎样本地验证”。这些内容可以用不同结构表达，不要求每篇套用相同标题。
4. 大仓库要维护覆盖清单：核心 crate、支撑 crate、构建 crate、生成代码、第三方 vendored 代码分别标注；没有展开的目录要说明原因，不能用一张简化架构图暗示已经读完全部实现。
5. 代码示例分为“源码摘录”和“伪代码”两类，必须注明类别；示例旁边写出文件路径和函数名，方便初学者从文章跳回真实实现。
6. 与 Pi 或其他项目对比时，先分别说明两边的实际实现，再比较抽象；不要把 Pi 的术语直接套在 Grok Build 上，也不要用“更先进”“更简单”替代具体差异。
7. 每次上游同步或发现源码链接失效时，优先修订正文中的事实、索引和 Q&A，并记录修订原因；不能只改目录名称而保留过时的章节内容。

8. Q&A 只收录读者（本项目中即作者）在学习过程中实际提出的问题，以及针对这些问题的回答。不能由写作者或助手根据正文主题预设、虚构或批量填充“可能会问的问题”；没有真实提问时，Q&A 保持为空白记录区。

9. 解释一个功能时不能只写“它做什么、代码在哪里”。至少要交代它要解决的约束、为什么选择当前实现、带来的好处、付出的代价、可能的替代方案，以及失败/边界场景；这些判断必须尽量回到源码、测试或用户指南。

10. 复杂功能要多种证据配合：用真实源码片段说明关键类型或函数，用明确标注的伪代码压缩调用链，用 Mermaid 表达时序/状态/数据流，再用表格或本地实验验证。图不是装饰，图下要说明它依据哪些文件、能证明什么、不能证明什么。

11. 可以关联网上的官方文档、协议规范、作者文章和社区实践，但必须标明资料角色、来源链接和访问日期；官方/第三方/社区资料只能补充背景、术语或使用经验，不能替代当前仓库快照对实现事实的证明。外部页面与本地源码不一致时，记录差异和版本，而不是把网页内容直接套回源码。
